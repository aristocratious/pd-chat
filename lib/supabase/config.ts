export const isSupabaseEnabled = false // Disabled for n8n backend

// Keep the original logic commented out for reference
// export const isSupabaseEnabled = !!(
//   process.env.NEXT_PUBLIC_SUPABASE_URL && 
//   process.env.NEXT_PUBLIC_SUPABASE_ANON_KEY
// )
